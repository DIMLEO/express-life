<!DOCTYPE html>
<html>
<head lang="en">
    <style>
        @font-face {
            font-family: 'Lato';
            font-style: normal;
            font-weight: 100;
            src: local('Lato Hairline'), local('Lato-Hairline'), url(@asset(css/fonts/font.woff2)) format('woff2');
        }
        html, body {
            height: 100%;
        }
        body {
            margin: 0;
            padding: 0;
            width: 100%;
            display: table;
            font-weight: 100;
            font-family: 'Lato';
        }
        .container {
            text-align: center;
            display: table-cell;
            vertical-align: middle;
        }
        .content {
            text-align: center;
            display: inline-block;
        }
        .title {
            font-size: 96px;
        }
    </style>
    <meta charset="UTF-8" />
    <title>${title}</title>
</head>
<body>

    @yield(body)

</body>
</html>